# Copy to .env.local (gitignored). Vite exposes only VITE_-prefixed vars to the client.
VITE_SUI_NETWORK=testnet
# Published Conduit package id (0x...), from `sui client publish`.
VITE_CONDUIT_PACKAGE_ID=
